#  პრეზენტაცია: Self-Work / Classwork
## JSONPlaceholder API + React Helmet + JSON-LD

---

## 🎯 სლაიდი 1: შესავალი / INTRODUCTION

### კლასის სამუშაო: React SEO და API ინტეგრაცია
### Classwork: React SEO and API Integration

**დრო / Duration:** 90 წუთი / minutes  
**დონე / Level:** საშუალო / Intermediate  
**ტექნოლოგიები / Technologies:**
- React 18+
- react-helmet-async
- JSONPlaceholder API
- JSON-LD

---

## 📋 სლაიდი 2: სამუშაოს მიზნები / OBJECTIVES

### რას ვისწავლით? / What will we learn?

✅ **API-დან მონაცემების მიღება** / Fetching data from API  
✅ **SEO მეტა თეგების დინამიური გენერაცია** / Dynamic SEO meta tags generation  
✅ **JSON-LD სტრუქტურირებული მონაცემები** / JSON-LD structured data  
✅ **პროდუქტების/პოსტების გვერდების ოპტიმიზაცია** / Product/Post pages optimization  
✅ **დინამიური routing და SEO** / Dynamic routing and SEO

---

## 🛠️ სლაიდი 3: საჭირო ინსტრუმენტები / REQUIRED TOOLS

### დაყენება / Setup

```bash
# 1. ახალი პროექტის შექმნა / Create new project
npm create vite@latest classwork-seo -- --template react
cd classwork-seo

# 2. საჭირო პაკეტების ინსტალაცია / Install packages
npm install react-helmet-async react-router-dom

# 3. პროექტის გაშვება / Run project
npm run dev
```

### პროექტის სტრუქტურა / Project Structure

```
classwork-seo/
├── src/
│   ├── components/
│   │   ├── SEO.jsx
│   │   ├── JSONLD.jsx
│   │   ├── PostCard.jsx
│   │   └── UserCard.jsx
│   ├── pages/
│   │   ├── HomePage.jsx
│   │   ├── PostsPage.jsx
│   │   ├── PostDetailPage.jsx
│   │   ├── UsersPage.jsx
│   │   └── UserDetailPage.jsx
│   ├── hooks/
│   │   ├── usePosts.js
│   │   └── useUsers.js
│   ├── App.jsx
│   └── main.jsx
└── package.json
```

---

## 🌐 სლაიდი 4: JSONPlaceholder API გაცნობა / API INTRODUCTION

### რა არის JSONPlaceholder? / What is JSONPlaceholder?

**JSONPlaceholder** არის უფასო ნლაინ REST API, რომელიც გამოიყენება ტესტირებისა და პროტოტიპირებისთვის.

**JSONPlaceholder** is a free online REST API for testing and prototyping.

### ძირითადი endpoint-ები / Main endpoints:

| Endpoint | აღწერა / Description | მაგალითი / Example |
|----------|---------------------|-------------------|
| `/posts` | პოსტები / Posts | https://jsonplaceholder.typicode.com/posts |
| `/comments` | კომენტარები / Comments | https://jsonplaceholder.typicode.com/comments |
| `/users` | მომხმარებლები / Users | https://jsonplaceholder.typicode.com/users |
| `/todos` | ამოცანები / Todos | https://jsonplaceholder.typicode.com/todos |

---

## 📊 სლაიდი 5: API მონაცემთა სტრუქტურა / API DATA STRUCTURE

### Posts API Response

```json
{
  "userId": 1,
  "id": 1,
  "title": "sunt aut facere repellat provident occaecati excepturi optio reprehenderit",
  "body": "quia et suscipit\nsuscipit recusandae consequuntur expedita et cum\nreprehenderit molestiae ut ut quas totam\nnostrum rerum est autem sunt rem eveniet architecto"
}
```

### Users API Response

```json
{
  "id": 1,
  "name": "Leanne Graham",
  "username": "Bret",
  "email": "Sincere@april.biz",
  "address": {
    "street": "Kulas Light",
    "suite": "Apt. 556",
    "city": "Gwenborough",
    "zipcode": "92998-3874",
    "geo": {
      "lat": "-37.3159",
      "lng": "81.1496"
    }
  },
  "phone": "1-770-736-8031 x56442",
  "website": "hildegard.org",
  "company": {
    "name": "Romaguera-Crona",
    "catchPhrase": "Multi-layered client-server neural-net",
    "bs": "harness real-time e-markets"
  }
}
```

---

##  სლაიდი 6: Custom Hooks API-სთვის / CUSTOM HOOKS FOR API

### usePosts Hook

```jsx
// src/hooks/usePosts.js
import { useState, useEffect } from 'react';

export const usePosts = () => {
  const [posts, setPosts] = useState([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  useEffect(() => {
    const fetchPosts = async () => {
      try {
        setLoading(true);
        const response = await fetch('https://jsonplaceholder.typicode.com/posts');
        
        if (!response.ok) {
          throw new Error('Failed to fetch posts');
        }
        
        const data = await response.json();
        setPosts(data);
        setError(null);
      } catch (err) {
        setError(err.message);
      } finally {
        setLoading(false);
      }
    };

    fetchPosts();
  }, []);

  return { posts, loading, error };
};

// კონკრეტული პოსტის მიღება / Get single post
export const usePost = (id) => {
  const [post, setPost] = useState(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  useEffect(() => {
    const fetchPost = async () => {
      try {
        setLoading(true);
        const response = await fetch(`https://jsonplaceholder.typicode.com/posts/${id}`);
        
        if (!response.ok) {
          throw new Error('Post not found');
        }
        
        const data = await response.json();
        setPost(data);
      } catch (err) {
        setError(err.message);
      } finally {
        setLoading(false);
      }
    };

    if (id) {
      fetchPost();
    }
  }, [id]);

  return { post, loading, error };
};
```

### useUsers Hook

```jsx
// src/hooks/useUsers.js
import { useState, useEffect } from 'react';

export const useUsers = () => {
  const [users, setUsers] = useState([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  useEffect(() => {
    const fetchUsers = async () => {
      try {
        setLoading(true);
        const response = await fetch('https://jsonplaceholder.typicode.com/users');
        
        if (!response.ok) {
          throw new Error('Failed to fetch users');
        }
        
        const data = await response.json();
        setUsers(data);
      } catch (err) {
        setError(err.message);
      } finally {
        setLoading(false);
      }
    };

    fetchUsers();
  }, []);

  return { users, loading, error };
};

export const useUser = (id) => {
  const [user, setUser] = useState(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  useEffect(() => {
    const fetchUser = async () => {
      try {
        setLoading(true);
        const response = await fetch(`https://jsonplaceholder.typicode.com/users/${id}`);
        
        if (!response.ok) {
          throw new Error('User not found');
        }
        
        const data = await response.json();
        setUser(data);
      } catch (err) {
        setError(err.message);
      } finally {
        setLoading(false);
      }
    };

    if (id) {
      fetchUser();
    }
  }, [id]);

  return { user, loading, error };
};
```

---

## 🎭 სლაიდი 7: SEO კომპონენტი / SEO COMPONENT

### უნივერსალური SEO კომპონენტი / Universal SEO Component

```jsx
// src/components/SEO.jsx
import React from 'react';
import { Helmet } from 'react-helmet-async';

const SEO = ({ 
  title, 
  description, 
  image, 
  url, 
  type = 'website',
  publishedTime = null,
  modifiedTime = null
}) => {
  const siteName = 'JSONPlaceholder Blog';
  const siteUrl = 'http://localhost:5173';
  const defaultImage = `${siteUrl}/default-image.jpg`;
  const fullUrl = url ? `${siteUrl}${url}` : siteUrl;

  return (
    <Helmet>
      {/* ძირითადი მეტა თეგები / Basic Meta Tags */}
      <title>{title} | {siteName}</title>
      <meta name="description" content={description} />
      <meta name="robots" content="index, follow" />
      <meta name="viewport" content="width=device-width, initial-scale=1.0" />
      
      {/* Open Graph / Facebook */}
      <meta property="og:type" content={type} />
      <meta property="og:url" content={fullUrl} />
      <meta property="og:title" content={title} />
      <meta property="og:description" content={description} />
      <meta property="og:image" content={image || defaultImage} />
      <meta property="og:site_name" content={siteName} />
      
      {publishedTime && (
        <meta property="article:published_time" content={publishedTime} />
      )}
      
      {modifiedTime && (
        <meta property="article:modified_time" content={modifiedTime} />
      )}
      
      {/* Twitter */}
      <meta name="twitter:card" content="summary_large_image" />
      <meta name="twitter:url" content={fullUrl} />
      <meta name="twitter:title" content={title} />
      <meta name="twitter:description" content={description} />
      <meta name="twitter:image" content={image || defaultImage} />
      
      {/* Canonical URL */}
      <link rel="canonical" href={fullUrl} />
    </Helmet>
  );
};

export default SEO;
```

---

##  სლაიდი 8: JSON-LD კომპონენტი / JSON-LD COMPONENT

### სტრუქტურირებული მონაცემები / Structured Data

```jsx
// src/components/JSONLD.jsx
import React from 'react';
import { Helmet } from 'react-helmet-async';

// ბლოგ პოსტის JSON-LD / Blog Post JSON-LD
export const BlogPostJSONLD = ({ post, author }) => {
  const jsonLd = {
    "@context": "https://schema.org",
    "@type": "BlogPosting",
    "headline": post.title,
    "description": post.body.substring(0, 150) + '...',
    "image": "http://localhost:5173/default-image.jpg",
    "author": {
      "@type": "Person",
      "name": author?.name || 'Unknown',
      "url": `http://localhost:5173/users/${author?.id}`
    },
    "publisher": {
      "@type": "Organization",
      "name": "JSONPlaceholder Blog",
      "logo": {
        "@type": "ImageObject",
        "url": "http://localhost:5173/logo.png"
      }
    },
    "datePublished": new Date().toISOString(),
    "dateModified": new Date().toISOString(),
    "mainEntityOfPage": {
      "@type": "WebPage",
      "@id": `http://localhost:5173/posts/${post.id}`
    }
  };

  return (
    <Helmet>
      <script type="application/ld+json">
        {JSON.stringify(jsonLd)}
      </script>
    </Helmet>
  );
};

// პროფილის JSON-LD / Profile JSON-LD
export const PersonJSONLD = ({ user }) => {
  const jsonLd = {
    "@context": "https://schema.org",
    "@type": "Person",
    "name": user.name,
    "url": `http://localhost:5173/users/${user.id}`,
    "email": user.email,
    "telephone": user.phone,
    "jobTitle": user.company?.catchPhrase,
    "worksFor": {
      "@type": "Organization",
      "name": user.company?.name
    },
    "address": {
      "@type": "PostalAddress",
      "streetAddress": user.address?.street,
      "addressLocality": user.address?.city,
      "postalCode": user.address?.zipcode
    }
  };

  return (
    <Helmet>
      <script type="application/ld+json">
        {JSON.stringify(jsonLd)}
      </script>
    </Helmet>
  );
};

// ვებგვერდის JSON-LD / Website JSON-LD
export const WebsiteJSONLD = () => {
  const jsonLd = {
    "@context": "https://schema.org",
    "@type": "WebSite",
    "name": "JSONPlaceholder Blog",
    "url": "http://localhost:5173",
    "potentialAction": {
      "@type": "SearchAction",
      "target": "http://localhost:5173/search?q={search_term_string}",
      "query-input": "required name=search_term_string"
    }
  };

  return (
    <Helmet>
      <script type="application/ld+json">
        {JSON.stringify(jsonLd)}
      </script>
    </Helmet>
  );
};

// Breadcrumb JSON-LD
export const BreadcrumbJSONLD = ({ items }) => {
  const jsonLd = {
    "@context": "https://schema.org",
    "@type": "BreadcrumbList",
    "itemListElement": items.map((item, index) => ({
      "@type": "ListItem",
      "position": index + 1,
      "name": item.name,
      "item": `http://localhost:5173${item.url}`
    }))
  };

  return (
    <Helmet>
      <script type="application/ld+json">
        {JSON.stringify(jsonLd)}
      </script>
    </Helmet>
  );
};
```

---

##  სლაიდი 9: მთავარი გვერდი / HOME PAGE

### HomePage Component

```jsx
// src/pages/HomePage.jsx
import React from 'react';
import { Link } from 'react-router-dom';
import SEO from '../components/SEO';
import { WebsiteJSONLD } from '../components/JSONLD';

const HomePage = () => {
  return (
    <>
      <SEO
        title="Home"
        description="Welcome to JSONPlaceholder Blog - Explore posts and users"
        url="/"
        type="website"
      />
      <WebsiteJSONLD />

      <div className="home-page">
        <header className="hero">
          <h1>JSONPlaceholder Blog</h1>
          <p>Explore posts, users, and comments with SEO optimization</p>
        </header>

        <nav className="home-nav">
          <Link to="/posts" className="nav-card">
            <h2> Posts</h2>
            <p>Browse all blog posts</p>
          </Link>

          <Link to="/users" className="nav-card">
            <h2>👥 Users</h2>
            <p>View all users</p>
          </Link>
        </nav>

        <section className="features">
          <h2>Features</h2>
          <ul>
            <li>✅ Dynamic SEO meta tags</li>
            <li>✅ JSON-LD structured data</li>
            <li>✅ React Helmet Async integration</li>
            <li>✅ API data fetching</li>
            <li>✅ Responsive design</li>
          </ul>
        </section>
      </div>
    </>
  );
};

export default HomePage;
```

---

## 📝 სლაიდი 10: პოსტების სია / POSTS LIST PAGE

### PostsPage Component

```jsx
// src/pages/PostsPage.jsx
import React from 'react';
import { Link } from 'react-router-dom';
import SEO from '../components/SEO';
import { BreadcrumbJSONLD } from '../components/JSONLD';
import { usePosts } from '../hooks/usePosts';
import PostCard from '../components/PostCard';

const PostsPage = () => {
  const { posts, loading, error } = usePosts();

  if (loading) {
    return (
      <div className="loading">
        <p>Loading posts...</p>
      </div>
    );
  }

  if (error) {
    return (
      <div className="error">
        <p>Error: {error}</p>
      </div>
    );
  }

  return (
    <>
      <SEO
        title="All Posts"
        description="Browse all blog posts from JSONPlaceholder API"
        url="/posts"
        type="website"
      />
      
      <BreadcrumbJSONLD items={[
        { name: 'Home', url: '/' },
        { name: 'Posts', url: '/posts' }
      ]} />

      <div className="posts-page">
        <h1>All Posts</h1>
        <p className="subtitle">Total: {posts.length} posts</p>

        <div className="posts-grid">
          {posts.map(post => (
            <PostCard key={post.id} post={post} />
          ))}
        </div>
      </div>
    </>
  );
};

export default PostsPage;
```

### PostCard Component

```jsx
// src/components/PostCard.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const PostCard = ({ post }) => {
  const truncatedTitle = post.title.length > 50 
    ? post.title.substring(0, 50) + '...' 
    : post.title;
  
  const truncatedBody = post.body.length > 100 
    ? post.body.substring(0, 100) + '...' 
    : post.body;

  return (
    <article className="post-card">
      <Link to={`/posts/${post.id}`} className="post-link">
        <h2>{truncatedTitle}</h2>
        <p>{truncatedBody}</p>
        <span className="read-more">Read more →</span>
      </Link>
    </article>
  );
};

export default PostCard;
```

---

## 📄 სლაიდი 11: პოსტის დეტალური გვერდი / POST DETAIL PAGE

### PostDetailPage Component

```jsx
// src/pages/PostDetailPage.jsx
import React from 'react';
import { useParams, Link } from 'react-router-dom';
import SEO from '../components/SEO';
import { BlogPostJSONLD, BreadcrumbJSONLD } from '../components/JSONLD';
import { usePost } from '../hooks/usePosts';
import { useUser } from '../hooks/useUsers';

const PostDetailPage = () => {
  const { id } = useParams();
  const { post, loading, error } = usePost(id);
  
  const authorId = post?.userId;
  const { user: author } = useUser(authorId);

  if (loading) {
    return <div className="loading">Loading post...</div>;
  }

  if (error) {
    return <div className="error">Error: {error}</div>;
  }

  if (!post) {
    return <div className="error">Post not found</div>;
  }

  return (
    <>
      <SEO
        title={post.title}
        description={post.body.substring(0, 150) + '...'}
        url={`/posts/${post.id}`}
        type="article"
        publishedTime={new Date().toISOString()}
      />

      <BlogPostJSONLD post={post} author={author} />

      <BreadcrumbJSONLD items={[
        { name: 'Home', url: '/' },
        { name: 'Posts', url: '/posts' },
        { name: post.title.substring(0, 30), url: `/posts/${post.id}` }
      ]} />

      <article className="post-detail">
        <header className="post-header">
          <Link to="/posts" className="back-link">← Back to Posts</Link>
          <h1>{post.title}</h1>
          
          {author && (
            <div className="post-meta">
              <span className="author">
                By <Link to={`/users/${author.id}`}>{author.name}</Link>
              </span>
              <span className="date">
                {new Date().toLocaleDateString()}
              </span>
            </div>
          )}
        </header>

        <div className="post-content">
          <p>{post.body}</p>
        </div>

        <footer className="post-footer">
          <div className="tags">
            <span className="tag">Post #{post.id}</span>
            <span className="tag">User #{post.userId}</span>
          </div>
        </footer>
      </article>
    </>
  );
};

export default PostDetailPage;
```

---

## 👥 სლაიდი 12: მომხმარებლების გვერდი / USERS PAGE

### UsersPage Component

```jsx
// src/pages/UsersPage.jsx
import React from 'react';
import { Link } from 'react-router-dom';
import SEO from '../components/SEO';
import { BreadcrumbJSONLD } from '../components/JSONLD';
import { useUsers } from '../hooks/useUsers';
import UserCard from '../components/UserCard';

const UsersPage = () => {
  const { users, loading, error } = useUsers();

  if (loading) {
    return <div className="loading">Loading users...</div>;
  }

  if (error) {
    return <div className="error">Error: {error}</div>;
  }

  return (
    <>
      <SEO
        title="All Users"
        description="Browse all users from JSONPlaceholder API"
        url="/users"
        type="website"
      />

      <BreadcrumbJSONLD items={[
        { name: 'Home', url: '/' },
        { name: 'Users', url: '/users' }
      ]} />

      <div className="users-page">
        <h1>All Users</h1>
        <p className="subtitle">Total: {users.length} users</p>

        <div className="users-grid">
          {users.map(user => (
            <UserCard key={user.id} user={user} />
          ))}
        </div>
      </div>
    </>
  );
};

export default UsersPage;
```

### UserCard Component

```jsx
// src/components/UserCard.jsx
import React from 'react';
import { Link } from 'react-router-dom';

const UserCard = ({ user }) => {
  return (
    <article className="user-card">
      <Link to={`/users/${user.id}`} className="user-link">
        <div className="user-avatar">
          {user.name.charAt(0).toUpperCase()}
        </div>
        
        <h2>{user.name}</h2>
        <p className="username">@{user.username}</p>
        
        <div className="user-info">
          <p className="email">📧 {user.email}</p>
          <p className="phone">📱 {user.phone}</p>
          <p className="company">🏢 {user.company?.name}</p>
        </div>
        
        <span className="view-profile">View Profile →</span>
      </Link>
    </article>
  );
};

export default UserCard;
```

---

## 👤 სლაიდი 13: მომხმარებლის პროფილი / USER PROFILE PAGE

### UserDetailPage Component

```jsx
// src/pages/UserDetailPage.jsx
import React from 'react';
import { useParams, Link } from 'react-router-dom';
import SEO from '../components/SEO';
import { PersonJSONLD, BreadcrumbJSONLD } from '../components/JSONLD';
import { useUser } from '../hooks/useUsers';

const UserDetailPage = () => {
  const { id } = useParams();
  const { user, loading, error } = useUser(id);

  if (loading) {
    return <div className="loading">Loading user profile...</div>;
  }

  if (error) {
    return <div className="error">Error: {error}</div>;
  }

  if (!user) {
    return <div className="error">User not found</div>;
  }

  return (
    <>
      <SEO
        title={`${user.name} - Profile`}
        description={`Profile of ${user.name} - ${user.company?.name}`}
        url={`/users/${user.id}`}
        type="profile"
      />

      <PersonJSONLD user={user} />

      <BreadcrumbJSONLD items={[
        { name: 'Home', url: '/' },
        { name: 'Users', url: '/users' },
        { name: user.name, url: `/users/${user.id}` }
      ]} />

      <div className="user-detail">
        <Link to="/users" className="back-link">← Back to Users</Link>

        <header className="profile-header">
          <div className="profile-avatar">
            {user.name.charAt(0).toUpperCase()}
          </div>
          <h1>{user.name}</h1>
          <p className="username">@{user.username}</p>
        </header>

        <div className="profile-content">
          <section className="profile-section">
            <h2>Contact Information</h2>
            <div className="info-grid">
              <div className="info-item">
                <span className="label">📧 Email</span>
                <span className="value">{user.email}</span>
              </div>
              <div className="info-item">
                <span className="label">📱 Phone</span>
                <span className="value">{user.phone}</span>
              </div>
              <div className="info-item">
                <span className="label">🌐 Website</span>
                <a href={`https://${user.website}`} target="_blank" rel="noopener noreferrer">
                  {user.website}
                </a>
              </div>
            </div>
          </section>

          <section className="profile-section">
            <h2>Address</h2>
            <div className="info-grid">
              <div className="info-item">
                <span className="label"> Street</span>
                <span className="value">{user.address?.street}</span>
              </div>
              <div className="info-item">
                <span className="label">️ City</span>
                <span className="value">{user.address?.city}</span>
              </div>
              <div className="info-item">
                <span className="label">📮 Zipcode</span>
                <span className="value">{user.address?.zipcode}</span>
              </div>
            </div>
          </section>

          <section className="profile-section">
            <h2>Company</h2>
            <div className="info-grid">
              <div className="info-item">
                <span className="label">🏢 Name</span>
                <span className="value">{user.company?.name}</span>
              </div>
              <div className="info-item">
                <span className="label">💼 Catch Phrase</span>
                <span className="value">{user.company?.catchPhrase}</span>
              </div>
              <div className="info-item">
                <span className="label">📊 BS</span>
                <span className="value">{user.company?.bs}</span>
              </div>
            </div>
          </section>
        </div>
      </div>
    </>
  );
};

export default UserDetailPage;
```

---

## 🎨 სლაიდი 14: CSS სტილები / CSS STYLES

### Main Styles

```css
/* src/App.css */

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', sans-serif;
  line-height: 1.6;
  color: #333;
  background: #f5f5f5;
}

.container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 2rem;
}

/* Home Page */
.home-page {
  text-align: center;
}

.hero {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  padding: 4rem 2rem;
  border-radius: 12px;
  margin-bottom: 3rem;
}

.hero h1 {
  font-size: 3rem;
  margin-bottom: 1rem;
}

.home-nav {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 2rem;
  margin-bottom: 3rem;
}

.nav-card {
  background: white;
  padding: 2rem;
  border-radius: 12px;
  text-decoration: none;
  color: #333;
  box-shadow: 0 4px 6px rgba(0,0,0,0.1);
  transition: transform 0.3s, box-shadow 0.3s;
}

.nav-card:hover {
  transform: translateY(-5px);
  box-shadow: 0 8px 16px rgba(0,0,0,0.15);
}

.nav-card h2 {
  font-size: 1.8rem;
  margin-bottom: 0.5rem;
}

/* Posts Grid */
.posts-grid,
.users-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
  gap: 2rem;
  margin-top: 2rem;
}

/* Post Card */
.post-card {
  background: white;
  padding: 1.5rem;
  border-radius: 12px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.1);
  transition: transform 0.3s;
}

.post-card:hover {
  transform: translateY(-3px);
}

.post-link {
  text-decoration: none;
  color: inherit;
}

.post-link h2 {
  font-size: 1.2rem;
  margin-bottom: 1rem;
  color: #667eea;
}

.post-link p {
  color: #666;
  margin-bottom: 1rem;
  line-height: 1.5;
}

.read-more {
  color: #667eea;
  font-weight: 600;
}

/* User Card */
.user-card {
  background: white;
  padding: 1.5rem;
  border-radius: 12px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.1);
  text-align: center;
}

.user-avatar {
  width: 80px;
  height: 80px;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 2rem;
  font-weight: bold;
  margin: 0 auto 1rem;
}

.user-link {
  text-decoration: none;
  color: inherit;
}

.username {
  color: #666;
  margin-bottom: 1rem;
}

.user-info {
  text-align: left;
  margin: 1rem 0;
}

.user-info p {
  font-size: 0.9rem;
  margin: 0.5rem 0;
  color: #555;
}

.view-profile {
  color: #667eea;
  font-weight: 600;
}

/* Post Detail */
.post-detail {
  background: white;
  padding: 3rem;
  border-radius: 12px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.1);
}

.post-header {
  margin-bottom: 2rem;
  padding-bottom: 2rem;
  border-bottom: 2px solid #f0f0f0;
}

.post-header h1 {
  font-size: 2.5rem;
  margin: 1rem 0;
  color: #333;
}

.post-meta {
  display: flex;
  gap: 2rem;
  color: #666;
}

.post-meta a {
  color: #667eea;
  text-decoration: none;
}

.post-content {
  font-size: 1.1rem;
  line-height: 1.8;
  color: #444;
}

.post-footer {
  margin-top: 3rem;
  padding-top: 2rem;
  border-top: 2px solid #f0f0f0;
}

.tags {
  display: flex;
  gap: 1rem;
}

.tag {
  background: #f0f0f0;
  padding: 0.5rem 1rem;
  border-radius: 20px;
  font-size: 0.9rem;
  color: #666;
}

/* User Detail */
.user-detail {
  background: white;
  padding: 3rem;
  border-radius: 12px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.1);
}

.profile-header {
  text-align: center;
  margin-bottom: 3rem;
  padding-bottom: 2rem;
  border-bottom: 2px solid #f0f0f0;
}

.profile-avatar {
  width: 120px;
  height: 120px;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 3rem;
  font-weight: bold;
  margin: 0 auto 1rem;
}

.profile-section {
  margin-bottom: 2rem;
}

.profile-section h2 {
  font-size: 1.5rem;
  margin-bottom: 1rem;
  color: #667eea;
}

.info-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 1rem;
}

.info-item {
  background: #f9f9f9;
  padding: 1rem;
  border-radius: 8px;
}

.info-item .label {
  display: block;
  font-size: 0.85rem;
  color: #666;
  margin-bottom: 0.5rem;
}

.info-item .value {
  font-size: 1rem;
  color: #333;
  font-weight: 500;
}

/* Back Link */
.back-link {
  display: inline-block;
  color: #667eea;
  text-decoration: none;
  margin-bottom: 2rem;
  font-weight: 600;
}

.back-link:hover {
  text-decoration: underline;
}

/* Loading & Error */
.loading,
.error {
  text-align: center;
  padding: 4rem 2rem;
  font-size: 1.2rem;
}

.error {
  color: #dc3545;
}

/* Responsive */
@media (max-width: 768px) {
  .hero h1 {
    font-size: 2rem;
  }
  
  .post-detail,
  .user-detail {
    padding: 1.5rem;
  }
  
  .post-header h1 {
    font-size: 1.8rem;
  }
}
```

---

## 🚦 სლაიდი 15: App.js და Routing / APP CONFIGURATION

### Main App Component

```jsx
// src/App.jsx
import React from 'react';
import { BrowserRouter as Router, Routes, Route } from 'react-router-dom';
import { HelmetProvider } from 'react-helmet-async';
import HomePage from './pages/HomePage';
import PostsPage from './pages/PostsPage';
import PostDetailPage from './pages/PostDetailPage';
import UsersPage from './pages/UsersPage';
import UserDetailPage from './pages/UserDetailPage';
import './App.css';

function App() {
  return (
    <HelmetProvider>
      <Router>
        <div className="container">
          <Routes>
            <Route path="/" element={<HomePage />} />
            <Route path="/posts" element={<PostsPage />} />
            <Route path="/posts/:id" element={<PostDetailPage />} />
            <Route path="/users" element={<UsersPage />} />
            <Route path="/users/:id" element={<UserDetailPage />} />
          </Routes>
        </div>
      </Router>
    </HelmetProvider>
  );
}

export default App;
```

### Main Entry Point

```jsx
// src/main.jsx
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App.jsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
```

---

## 📋 სლაიდი 16: Self-Work დავალებები / SELF-WORK TASKS

### დავალება 1: კომენტარების გვერდი / Task 1: Comments Page

**აღწერა / Description:**
შექმენით კომენტარების გვერდი კონკრეტული პოსტისთვის.

**Create a comments page for a specific post.**

**მოთხოვნები / Requirements:**
- ✅ გამოიყენეთ `/comments?postId={id}` endpoint
- ✅ დაამატეთ SEO მეტა თეგები
- ✅ დაამატეთ JSON-LD სტრუქტურა
- ✅ გამოიყენეთ Breadcrumb navigation

**კოდის ჩარჩო / Code Skeleton:**

```jsx
// src/pages/CommentsPage.jsx
import React from 'react';
import { useParams, Link } from 'react-router-dom';
import SEO from '../components/SEO';
import { BreadcrumbJSONLD } from '../components/JSONLD';

const CommentsPage = () => {
  const { postId } = useParams();
  const [comments, setComments] = React.useState([]);
  const [loading, setLoading] = React.useState(true);

  React.useEffect(() => {
    const fetchComments = async () => {
      try {
        const response = await fetch(
          `https://jsonplaceholder.typicode.com/comments?postId=${postId}`
        );
        const data = await response.json();
        setComments(data);
      } catch (error) {
        console.error('Error fetching comments:', error);
      } finally {
        setLoading(false);
      }
    };

    fetchComments();
  }, [postId]);

  if (loading) return <div>Loading comments...</div>;

  return (
    <>
      <SEO
        title={`Comments for Post #${postId}`}
        description={`Read all comments for post ${postId}`}
        url={`/posts/${postId}/comments`}
      />
      
      <BreadcrumbJSONLD items={[
        { name: 'Home', url: '/' },
        { name: 'Posts', url: '/posts' },
        { name: `Post #${postId}`, url: `/posts/${postId}` },
        { name: 'Comments', url: `/posts/${postId}/comments` }
      ]} />

      <div className="comments-page">
        <h1>Comments for Post #{postId}</h1>
        <Link to={`/posts/${postId}`} className="back-link">← Back to Post</Link>
        
        <div className="comments-list">
          {comments.map(comment => (
            <article key={comment.id} className="comment-card">
              <h3>{comment.name}</h3>
              <p className="comment-email">{comment.email}</p>
              <p className="comment-body">{comment.body}</p>
            </article>
          ))}
        </div>
      </div>
    </>
  );
};

export default CommentsPage;
```

---

## 📋 სლაიდი 17: Self-Work დავალებები / SELF-WORK TASKS (CONTINUED)

### დავალება 2: ძიების ფუნქციონალი / Task 2: Search Functionality

**აღწერა / Description:**
დაამატეთ ძიების ფუნქცია პოსტების გვერდზე.

**Add search functionality to the posts page.**

**მოთხოვნები / Requirements:**
- ✅ შექმენით search input ველი
- ✅ ფილტრაცია client-side
- ✅ დინამიური SEO title განახლება
- ✅ შედეგების რაოდენობის ჩვენება

**კოდის ჩარჩო / Code Skeleton:**

```jsx
// src/pages/PostsPage.jsx (განახლებული / Updated)
import React, { useState, useMemo } from 'react';

const PostsPage = () => {
  const { posts, loading, error } = usePosts();
  const [searchQuery, setSearchQuery] = useState('');

  const filteredPosts = useMemo(() => {
    if (!searchQuery.trim()) return posts;
    
    const query = searchQuery.toLowerCase();
    return posts.filter(post =>
      post.title.toLowerCase().includes(query) ||
      post.body.toLowerCase().includes(query)
    );
  }, [posts, searchQuery]);

  return (
    <>
      <SEO
        title={searchQuery ? `Search: ${searchQuery}` : 'All Posts'}
        description={
          searchQuery 
            ? `Search results for "${searchQuery}"`
            : 'Browse all blog posts'
        }
        url={searchQuery ? `/posts?search=${searchQuery}` : '/posts'}
      />

      <div className="posts-page">
        <h1>All Posts</h1>
        
        <div className="search-container">
          <input
            type="text"
            placeholder="Search posts..."
            value={searchQuery}
            onChange={(e) => setSearchQuery(e.target.value)}
            className="search-input"
          />
        </div>

        <p className="results-count">
          Found {filteredPosts.length} posts
          {searchQuery && ` for "${searchQuery}"`}
        </p>

        <div className="posts-grid">
          {filteredPosts.map(post => (
            <PostCard key={post.id} post={post} />
          ))}
        </div>
      </div>
    </>
  );
};
```

---

## 📋 სლაიდი 18: Self-Work დავალებები / SELF-WORK TASKS (FINAL)

### დავალება 3: Todos გვერდი / Task 3: Todos Page

**აღწერა / Description:**
შექმენით Todos გვერდი ფილტრაციით (ყველა/აქტიური/დასრულებული).

**Create a Todos page with filtering (all/active/completed).**

**მოთხოვნები / Requirements:**
- ✅ გამოიყენეთ `/todos` endpoint
- ✅ ფილტრაცია სტატუსით
- ✅ პროგრესის ბარი
- ✅ SEO ოპტიმიზაცია

**კოდის ჩარჩო / Code Skeleton:**

```jsx
// src/pages/TodosPage.jsx
import React, { useState, useMemo } from 'react';
import SEO from '../components/SEO';

const TodosPage = () => {
  const [todos, setTodos] = React.useState([]);
  const [filter, setFilter] = useState('all');
  const [loading, setLoading] = React.useState(true);

  React.useEffect(() => {
    const fetchTodos = async () => {
      try {
        const response = await fetch('https://jsonplaceholder.typicode.com/todos');
        const data = await response.json();
        setTodos(data);
      } catch (error) {
        console.error('Error:', error);
      } finally {
        setLoading(false);
      }
    };

    fetchTodos();
  }, []);

  const filteredTodos = useMemo(() => {
    switch (filter) {
      case 'active':
        return todos.filter(todo => !todo.completed);
      case 'completed':
        return todos.filter(todo => todo.completed);
      default:
        return todos;
    }
  }, [todos, filter]);

  const completedCount = todos.filter(t => t.completed).length;
  const progress = (completedCount / todos.length) * 100;

  if (loading) return <div>Loading todos...</div>;

  return (
    <>
      <SEO
        title={`Todos - ${filter.charAt(0).toUpperCase() + filter.slice(1)}`}
        description={`Manage your todos - ${completedCount} of ${todos.length} completed`}
        url="/todos"
      />

      <div className="todos-page">
        <h1>My Todos</h1>

        <div className="progress-container">
          <div className="progress-bar">
            <div 
              className="progress-fill" 
              style={{ width: `${progress}%` }}
            ></div>
          </div>
          <p>{completedCount} / {todos.length} completed ({Math.round(progress)}%)</p>
        </div>

        <div className="filter-buttons">
          <button 
            className={filter === 'all' ? 'active' : ''}
            onClick={() => setFilter('all')}
          >
            All ({todos.length})
          </button>
          <button 
            className={filter === 'active' ? 'active' : ''}
            onClick={() => setFilter('active')}
          >
            Active ({todos.length - completedCount})
          </button>
          <button 
            className={filter === 'completed' ? 'active' : ''}
            onClick={() => setFilter('completed')}
          >
            Completed ({completedCount})
          </button>
        </div>

        <ul className="todos-list">
          {filteredTodos.map(todo => (
            <li key={todo.id} className={`todo-item ${todo.completed ? 'completed' : ''}`}>
              <span className="todo-title">{todo.title}</span>
              <span className="todo-status">
                {todo.completed ? '✓' : '○'}
              </span>
            </li>
          ))}
        </ul>
      </div>
    </>
  );
};

export default TodosPage;
```

---

## 🎯 სლაიდი 19: შეფასების კრიტერიუმები / GRADING CRITERIA

### როგორ შეფასდება თქვენი სამუშაო? / How will your work be graded?

| კრიტერიუმი / Criteria | წონა / Weight | აღწერა / Description |
|----------------------|--------------|---------------------|
| **ფუნქციონალი** / Functionality | 30% | ყველა ფუნქცია მუშაობს / All features work |
| **SEO ოპტიმიზაცია** / SEO | 25% | მეტა თეგები და JSON-LD / Meta tags and JSON-LD |
| **კოდის ხარისხი** / Code Quality | 20% | კარგი სტრუქტურა და კომენტარები / Good structure and comments |
| **დიზაინი** / Design | 15% | ლამაზი და responsive / Beautiful and responsive |
| **დამატებითი ფუნქციები** / Bonus | 10% | დამატებითი ფუნქციონალი / Extra features |

### შეფასების სკალა / Grading Scale:

- **90-100%** - შესანიშნავი / Excellent ⭐⭐⭐⭐
- **80-89%** - კარგი / Good ⭐⭐⭐⭐
- **70-79%** - საკმარისი / Satisfactory ⭐⭐⭐
- **60-69%** - საჭიროებს გაუმჯობესებას / Needs Improvement ⭐⭐
- **<60%** - ვერ ჩაბარდა / Failed ⭐

---

## 📚 სლაიდი 20: დამატებითი რესურსები / ADDITIONAL RESOURCES

### სასარგებლო ლინკები / Useful Links

 **დოკუმენტაცია / Documentation:**
- [React Helmet Async](https://github.com/staylor/react-helmet-async)
- [JSONPlaceholder API](https://jsonplaceholder.typicode.com/)
- [Schema.org](https://schema.org/)
- [Google Structured Data](https://developers.google.com/search/docs/appearance/structured-data)

🎓 **სასწავლო მასალები / Learning Materials:**
- [React Official Docs](https://react.dev/)
- [React Router Docs](https://reactrouter.com/)
- [SEO Best Practices](https://web.dev/learn/seo/)

🛠️ **ინსტრუმენტები / Tools:**
- [Google Rich Results Test](https://search.google.com/test/rich-results)
- [Schema Markup Validator](https://validator.schema.org/)
- [React DevTools](https://react.dev/learn/react-developer-tools)

---

## ✅ სლაიდი 21: ჩეკლისტი / CHECKLIST

### დავალების შესრულების ჩეკლისტი / Task Completion Checklist

- [ ] ✅ პროექტი შექმნილია Vite-ით / Project created with Vite
- [ ] ✅ react-helmet-async ინსტალირებულია / react-helmet-async installed
- [ ] ✅ react-router-dom ინსტალირებულია / react-router-dom installed
- [ ] ✅ HomePage შექმნილია / HomePage created
- [ ] ✅ PostsPage შექმნილია / PostsPage created
- [ ] ✅ PostDetailPage შექმნილია / PostDetailPage created
- [ ] ✅ UsersPage შექმნილია / UsersPage created
- [ ] ✅ UserDetailPage შექმნილია / UserDetailPage created
- [ ] ✅ SEO კომპონენტი მუშაობს / SEO component works
- [ ] ✅ JSON-LD დამატებულია / JSON-LD added
- [ ] ✅ Breadcrumb navigation მუშაობს / Breadcrumb navigation works
- [ ] ✅ CSS სტილები დამატებულია / CSS styles added
- [ ] ✅ Responsive design განხორციელებულია / Responsive design implemented
- [ ] ✅ Loading და Error states დამატებულია / Loading and Error states added
- [ ] ✅ დამატებითი დავალებები შესრულებულია / Bonus tasks completed

---

## 🎉 სლაიდი 22: დასკვნა / CONCLUSION

### რა ვისწავლეთ? / What did we learn?

✅ **API-დან მონაცემების მიღება** / Fetching data from API  
✅ **დინამიური SEO მეტა თეგები** / Dynamic SEO meta tags  
✅ **JSON-LD სტრუქტურირებული მონაცემები** / JSON-LD structured data  
✅ **React Router-ის გამოყენება** / Using React Router  
✅ **Custom Hooks-ის შექმნა** / Creating Custom Hooks  
✅ **კომპონენტების სტრუქტურირება** / Component structuring  
✅ **Responsive design** / Responsive design  

### შემდეგი ნაბიჯები / Next Steps:

🚀 **გააუმჯობესეთ პროექტი** / Improve the project:
- დაამატეთ authentication / Add authentication
- გამოიყენეთ real backend / Use real backend
- დაამატეთ caching / Add caching
- implement pagination / Implement pagination
- დაამატეთ tests / Add tests

---

## 📝 სლაიდი 23: დავალების ჩაბარება / SUBMISSION

### როგორ ჩააბაროთ დავალება? / How to submit your work?

1. **GitHub Repository:**
   - ატვირთეთ კოდი GitHub-ზე / Upload code to GitHub
   - დაწერეთ კარგი README / Write a good README
   - დაამატეთ სქრინშოთები / Add screenshots

2. **დემო ვერსია:**
   - განათავსეთ Vercel/Netlify-ზე / Deploy to Vercel/Netlify
   - მიუთითეთ ლინკი / Provide the link

3. **დოკუმენტაცია:**
   - აღწერეთ რა გააკეთეთ / Describe what you did
   - ჩამოთვალეთ ფუნქციები / List features
   - მიუთითეთ გამოწვევები / Mention challenges

### ვადა / Deadline:
**7 დღე / 7 days from today**


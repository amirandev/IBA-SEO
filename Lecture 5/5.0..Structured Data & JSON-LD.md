# 📊 პრეზენტაცია / PRESENTATION

## **React პროექტის დაყენება SEO-ით და მონაცემების მართვა**
## **React Project Setup with SEO & Data Management**

---

## 📑 შინაარსი / CONTENTS

1. React პროექტის შექმნა / React Project Setup
2. react-helmet-async-ის ინტეგრაცია / Helmet-Async Integration
3. Meta Tags კომპონენტი / Meta Tags Component
4. JSON-LD სტრუქტურირებული მონაცემები / JSON-LD Structured Data
5. მონაცემების მიღება JSON ფაილიდან / Fetching Data from JSON
6. E-commerce აპლიკაცია / E-commerce Application
7. დებაგინგი / Debugging React Projects
8. მულტიენოვანი მხარდაჭერა (KA/EN) / Multilingual Support

---

## 1️⃣ REACT პროექტის შექმნა / SETUP

### ნაბიჯ-ნაბიჯ ინსტრუქცია / Step-by-Step Guide

```bash
# ახალი React პროექტის შექმნა / Create new React project
npx create-react-app my-ecommerce
cd my-ecommerce

# საჭირო პაკეტების ინსტალაცია / Install required packages
npm install react-helmet-async react-router-dom

# ან Vite-ით (რეკომენდებული) / Or with Vite (recommended)
npm create vite@latest my-ecommerce -- --template react
cd my-ecommerce
npm install
npm install react-helmet-async react-router-dom
```

---

## 2️⃣ REACT-HELMET-ASYNC ინსტალაცია / INSTALLATION

### ძირითადი კონფიგურაცია / Basic Configuration

```jsx
// src/App.js
import React from 'react';
import { HelmetProvider } from 'react-helmet-async';
import { BrowserRouter } from 'react-router-dom';
import App from './App';

const Root = () => (
  <HelmetProvider>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </HelmetProvider>
);

export default Root;
```

---

## 3️⃣ META TAGS კომპონენტი / META TAGS COMPONENT

### უნივერსალური SEO კომპონენტი / Universal SEO Component

```jsx
// src/components/SEO.js
import React from 'react';
import { Helmet } from 'react-helmet-async';

const SEO = ({ 
  title, 
  description, 
  image, 
  url, 
  article = false,
  publishedTime = null
}) => {
  const siteUrl = 'https://yourdomain.ge';
  const defaultImage = `${siteUrl}/default-image.jpg`;

  return (
    <Helmet>
      {/* ძირითადი მეტა თეგები / Basic Meta Tags */}
      <title>{title} | თქვენი მაღაზია</title>
      <meta name="description" content={description} />
      <meta name="robots" content="index, follow" />
      
      {/* Open Graph / Facebook */}
      <meta property="og:type" content={article ? "article" : "website"} />
      <meta property="og:url" content={url || siteUrl} />
      <meta property="og:title" content={title} />
      <meta property="og:description" content={description} />
      <meta property="og:image" content={image || defaultImage} />
      
      {/* Twitter */}
      <meta property="twitter:card" content="summary_large_image" />
      <meta property="twitter:url" content={url || siteUrl} />
      <meta property="twitter:title" content={title} />
      <meta property="twitter:description" content={description} />
      <meta property="twitter:image" content={image || defaultImage} />
      
      {/* Canonical URL */}
      <link rel="canonical" href={url || siteUrl} />
    </Helmet>
  );
};

export default SEO;
```

---

## 4️ JSON-LD სტრუქტურირებული მონაცემები / STRUCTURED DATA

### JSON-LD კომპონენტი / JSON-LD Component

```jsx
// src/components/JSONLD.js
import React from 'react';
import { Helmet } from 'react-helmet-async';

// პროდუქტის JSON-LD / Product JSON-LD
export const ProductJSONLD = ({ product }) => {
  const jsonLd = {
    "@context": "https://schema.org/",
    "@type": "Product",
    "name": product.name,
    "image": product.images,
    "description": product.description,
    "sku": product.sku,
    "brand": {
      "@type": "Brand",
      "name": product.brand
    },
    "offers": {
      "@type": "Offer",
      "url": product.url,
      "priceCurrency": "GEL",
      "price": product.price,
      "availability": product.inStock 
        ? "https://schema.org/InStock" 
        : "https://schema.org/OutOfStock",
      "seller": {
        "@type": "Organization",
        "name": "თქვენი მაღაზია"
      }
    },
    "aggregateRating": {
      "@type": "AggregateRating",
      "ratingValue": product.rating,
      "reviewCount": product.reviewCount
    }
  };

  return (
    <Helmet>
      <script type="application/ld+json">
        {JSON.stringify(jsonLd)}
      </script>
    </Helmet>
  );
};

// ვებგვერდის JSON-LD / Website JSON-LD
export const WebsiteJSONLD = () => {
  const jsonLd = {
    "@context": "https://schema.org",
    "@type": "WebSite",
    "name": "თქვენი E-commerce საიტი",
    "url": "https://yourdomain.ge",
    "potentialAction": {
      "@type": "SearchAction",
      "target": "https://yourdomain.ge/search?q={search_term_string}",
      "query-input": "required name=search_term_string"
    }
  };

  return (
    <Helmet>
      <script type="application/ld+json">
        {JSON.stringify(jsonLd)}
      </script>
    </Helmet>
  );
};

// Breadcrumb JSON-LD
export const BreadcrumbJSONLD = ({ items }) => {
  const jsonLd = {
    "@context": "https://schema.org",
    "@type": "BreadcrumbList",
    "itemListElement": items.map((item, index) => ({
      "@type": "ListItem",
      "position": index + 1,
      "name": item.name,
      "item": item.url
    }))
  };

  return (
    <Helmet>
      <script type="application/ld+json">
        {JSON.stringify(jsonLd)}
      </script>
    </Helmet>
  );
};
```

---

## 5️ მონაცემების მიღება JSON ფაილიდან / FETCHING DATA

### JSON მონაცემთა ბაზა / JSON Database

```json
// src/data/products.json
{
  "products": [
    {
      "id": 1,
      "sku": "LAPTOP-001",
      "name": {
        "ka": "MacBook Pro 16",
        "en": "MacBook Pro 16"
      },
      "description": {
        "ka": "ძლიერი ლეპტოპი პროფესიონალებისთვის",
        "en": "Powerful laptop for professionals"
      },
      "price": 4500,
      "currency": "GEL",
      "category": "laptops",
      "brand": "Apple",
      "images": [
        "/images/macbook-1.jpg",
        "/images/macbook-2.jpg"
      ],
      "inStock": true,
      "stock": 15,
      "rating": 4.8,
      "reviewCount": 124,
      "specifications": {
        "processor": "M2 Pro",
        "ram": "16GB",
        "storage": "512GB SSD"
      }
    },
    {
      "id": 2,
      "sku": "PHONE-001",
      "name": {
        "ka": "iPhone 15 Pro",
        "en": "iPhone 15 Pro"
      },
      "description": {
        "ka": "უახლესი iPhone ტიტანის კორპუსით",
        "en": "Latest iPhone with titanium body"
      },
      "price": 2800,
      "currency": "GEL",
      "category": "phones",
      "brand": "Apple",
      "images": ["/images/iphone-1.jpg"],
      "inStock": true,
      "stock": 30,
      "rating": 4.9,
      "reviewCount": 256
    }
  ],
  "categories": [
    {
      "id": 1,
      "name": {
        "ka": "ლეპტოპები",
        "en": "Laptops"
      },
      "slug": "laptops"
    },
    {
      "id": 2,
      "name": {
        "ka": "სმარტფონები",
        "en": "Smartphones"
      },
      "slug": "phones"
    }
  ]
}
```

### მონაცემების მიღების უქი / Data Fetching Hook

```jsx
// src/hooks/useProducts.js
import { useState, useEffect } from 'react';

export const useProducts = () => {
  const [products, setProducts] = useState([]);
  const [categories, setCategories] = useState([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  useEffect(() => {
    const fetchData = async () => {
      try {
        setLoading(true);
        const response = await fetch('/data/products.json');
        
        if (!response.ok) {
          throw new Error('მონაცემების ჩატვირთვა ვერ მოხერხდა');
        }
        
        const data = await response.json();
        setProducts(data.products);
        setCategories(data.categories);
        setError(null);
      } catch (err) {
        setError(err.message);
      } finally {
        setLoading(false);
      }
    };

    fetchData();
  }, []);

  return { products, categories, loading, error };
};

// კონკრეტული პროდუქტის მიღება / Get single product
export const useProduct = (id) => {
  const [product, setProduct] = useState(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  useEffect(() => {
    const fetchProduct = async () => {
      try {
        setLoading(true);
        const response = await fetch('/data/products.json');
        const data = await response.json();
        const foundProduct = data.products.find(p => p.id === parseInt(id));
        
        if (!foundProduct) {
          throw new Error('პროდუქტი ვერ მოიძებნა');
        }
        
        setProduct(foundProduct);
      } catch (err) {
        setError(err.message);
      } finally {
        setLoading(false);
      }
    };

    if (id) {
      fetchProduct();
    }
  }, [id]);

  return { product, loading, error };
};
```

---

## 6️⃣ E-COMMERCE აპლიკაცია / E-COMMERCE APPLICATION

### პროდუქტის გვერდი / Product Page

```jsx
// src/pages/ProductPage.js
import React, { useState } from 'react';
import { useParams } from 'react-router-dom';
import { Helmet } from 'react-helmet-async';
import SEO from '../components/SEO';
import { ProductJSONLD } from '../components/JSONLD';
import { useProduct } from '../hooks/useProducts';
import { useLanguage } from '../contexts/LanguageContext';

const ProductPage = () => {
  const { id } = useParams();
  const { product, loading, error } = useProduct(id);
  const { lang } = useLanguage();
  const [selectedImage, setSelectedImage] = useState(0);

  if (loading) return <div className="loading">იტვირთება...</div>;
  if (error) return <div className="error">შეცდომა: {error}</div>;
  if (!product) return <div>პროდუქტი არ მოიძებნა</div>;

  const productUrl = `https://yourdomain.ge/products/${product.id}`;
  
  return (
    <>
      <SEO
        title={product.name[lang]}
        description={product.description[lang]}
        image={product.images[0]}
        url={productUrl}
        article={false}
      />
      
      <ProductJSONLD product={{
        ...product,
        name: product.name[lang],
        description: product.description[lang],
        url: productUrl
      }} />

      <div className="product-page">
        <div className="product-images">
          <img 
            src={product.images[selectedImage]} 
            alt={product.name[lang]}
            className="main-image"
          />
          <div className="thumbnail-gallery">
            {product.images.map((img, index) => (
              <img
                key={index}
                src={img}
                alt={`${product.name[lang]} ${index + 1}`}
                className={`thumbnail ${selectedImage === index ? 'active' : ''}`}
                onClick={() => setSelectedImage(index)}
              />
            ))}
          </div>
        </div>

        <div className="product-info">
          <h1>{product.name[lang]}</h1>
          <p className="price">{product.price} ₾</p>
          <p className="description">{product.description[lang]}</p>
          
          <div className="rating">
            ⭐ {product.rating} ({product.reviewCount} შეფასება)
          </div>

          <div className="stock">
            {product.inStock ? (
              <span className="in-stock">✓ მარაგშია ({product.stock})</span>
            ) : (
              <span className="out-of-stock"> არ არის მარაგში</span>
            )}
          </div>

          <button className="add-to-cart">
            კალათაში დამატება
          </button>
        </div>

        <div className="specifications">
          <h2>მონაცემები / Specifications</h2>
          <ul>
            {Object.entries(product.specifications).map(([key, value]) => (
              <li key={key}>
                <strong>{key}:</strong> {value}
              </li>
            ))}
          </ul>
        </div>
      </div>
    </>
  );
};

export default ProductPage;
```

---

## 7️ დებაგინგი / DEBUGGING REACT PROJECTS

### დებაგინგის ხელსაწყოები / Debugging Tools

```jsx
// 1. Console.log სტრატეგიები / Console.log Strategies

// კომპონენტის lifecycle-ის თვალყურის დევნება
const ProductPage = ({ id }) => {
  const [product, setProduct] = useState(null);
  
  console.log('🔍 ProductPage rendered with id:', id);
  
  useEffect(() => {
    console.log('📥 useEffect triggered - fetching product:', id);
    
    fetchProduct(id).then(data => {
      console.log('✅ Product fetched:', data);
      setProduct(data);
    }).catch(error => {
      console.error('❌ Error fetching product:', error);
    });
    
    return () => {
      console.log(' Cleanup for product:', id);
    };
  }, [id]);

  console.log('📊 Current product state:', product);
  
  return <div>...</div>;
};

// 2. Error Boundaries / შეცდომების დამჭერი
class ErrorBoundary extends React.Component {
  constructor(props) {
    super(props);
    this.state = { hasError: false, error: null };
  }

  static getDerivedStateFromError(error) {
    console.error(' ErrorBoundary caught error:', error);
    return { hasError: true, error };
  }

  componentDidCatch(error, errorInfo) {
    console.error('🚨 ComponentStack:', errorInfo.componentStack);
    // შეგიძლიათ გაუგზავნოთ სერვერს / Send to server
    logErrorToService(error, errorInfo);
  }

  render() {
    if (this.state.hasError) {
      return (
        <div className="error-fallback">
          <h2>რაღაც შეცდომა მოხდა</h2>
          <p>{this.state.error?.toString()}</p>
          <button onClick={() => window.location.reload()}>
            გადატვირთვა
          </button>
        </div>
      );
    }

    return this.props.children;
  }
}

// 3. React DevTools პროფილირება / Profiling
// გამოიყენეთ React DevTools-ის "Profiler" ტაბი
// ჩაწერეთ რენდერინგის დრო და იპოვეთ ბოთლneck-ები

// 4. Custom Debug Hook
const useDebug = (componentName, props) => {
  useEffect(() => {
    console.log(`[${componentName}] Props changed:`, props);
  }, [props]);

  useEffect(() => {
    console.log(`[${componentName}] Mounted`);
    return () => console.log(`[${componentName}] Unmounted`);
  }, [componentName]);
};

// გამოყენება / Usage:
const MyComponent = (props) => {
  useDebug('MyComponent', props);
  // ...
};

// 5. Network Requests Debugging
const useDebugFetch = (url) => {
  const [data, setData] = useState(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  useEffect(() => {
    console.log(`🌐 Fetching from: ${url}`);
    const startTime = Date.now();
    
    fetch(url)
      .then(response => {
        console.log(`📦 Response status:`, response.status);
        return response.json();
      })
      .then(data => {
        const duration = Date.now() - startTime;
        console.log(`✅ Data received in ${duration}ms:`, data);
        setData(data);
      })
      .catch(error => {
        console.error(`❌ Fetch error:`, error);
        setError(error);
      })
      .finally(() => {
        setLoading(false);
      });
  }, [url]);

  return { data, loading, error };
};
```

### დებაგინგის საუკეთესო პრაქტიკები / Best Practices

```javascript
// src/utils/debug.js

// 1. Conditional Logging (მხოლოდ development-ში)
export const debug = {
  log: (...args) => {
    if (process.env.NODE_ENV === 'development') {
      console.log('🔵', ...args);
    }
  },
  error: (...args) => {
    console.error('🔴', ...args);
  },
  warn: (...args) => {
    console.warn('🟡', ...args);
  }
};

// 2. Performance Monitoring
export const measureRender = (componentName) => {
  const start = performance.now();
  
  return () => {
    const end = performance.now();
    console.log(`️ ${componentName} rendered in ${(end - start).toFixed(2)}ms`);
  };
};

// 3. State Inspector
export const useStateDebug = (initialState, stateName) => {
  const [state, setState] = useState(initialState);
  
  const debugSetState = (newState) => {
    console.log(` ${stateName} state change:`, {
      previous: state,
      next: newState
    });
    setState(newState);
  };
  
  return [state, debugSetState];
};
```

---

## 8️⃣ მულტიენოვანი მხარდაჭერა / MULTILINGUAL SUPPORT

### ენის კონტექსტი / Language Context

```jsx
// src/contexts/LanguageContext.js
import React, { createContext, useContext, useState, useEffect } from 'react';

const LanguageContext = createContext();

export const LanguageProvider = ({ children }) => {
  const [lang, setLang] = useState(() => {
    // შეინახეთ ენა localStorage-ში
    const saved = localStorage.getItem('language');
    return saved || 'ka';
  });

  useEffect(() => {
    localStorage.setItem('language', lang);
    document.documentElement.lang = lang;
  }, [lang]);

  const toggleLanguage = () => {
    setLang(prev => prev === 'ka' ? 'en' : 'ka');
  };

  return (
    <LanguageContext.Provider value={{ lang, setLang, toggleLanguage }}>
      {children}
    </LanguageContext.Provider>
  );
};

export const useLanguage = () => {
  const context = useContext(LanguageContext);
  if (!context) {
    throw new Error('useLanguage must be used within LanguageProvider');
  }
  return context;
};
```

### თარგმანების ფაილი / Translations File

```json
// src/translations/index.json
{
  "ka": {
    "nav": {
      "home": "მთავარი",
      "products": "პროდუქტები",
      "cart": "კალათა",
      "contact": "კონტაქტი"
    },
    "common": {
      "loading": "იტვირთება...",
      "error": "შეცდომა",
      "addToCart": "კალათაში დამატება",
      "buyNow": "ყიდვა",
      "inStock": "მარაგშია",
      "outOfStock": "არ არის მარაგში"
    },
    "product": {
      "specifications": "მონაცემები",
      "description": "აღწერა",
      "reviews": "შეფასებები",
      "relatedProducts": "მსგავსი პროდუქტები"
    }
  },
  "en": {
    "nav": {
      "home": "Home",
      "products": "Products",
      "cart": "Cart",
      "contact": "Contact"
    },
    "common": {
      "loading": "Loading...",
      "error": "Error",
      "addToCart": "Add to Cart",
      "buyNow": "Buy Now",
      "inStock": "In Stock",
      "outOfStock": "Out of Stock"
    },
    "product": {
      "specifications": "Specifications",
      "description": "Description",
      "reviews": "Reviews",
      "relatedProducts": "Related Products"
    }
  }
}
```

### ენის გადამრთველი კომპონენტი / Language Switcher Component

```jsx
// src/components/LanguageSwitcher.js
import React from 'react';
import { useLanguage } from '../contexts/LanguageContext';

const LanguageSwitcher = () => {
  const { lang, toggleLanguage } = useLanguage();

  return (
    <div className="language-switcher">
      <button
        onClick={toggleLanguage}
        className={`lang-btn ${lang}`}
        aria-label={lang === 'ka' ? 'Switch to English' : 'ქართულად'}
      >
        {lang === 'ka' ? (
          <>
            <span className="flag">🇬</span>
            <span>ქართული</span>
            <span className="switch-to">EN</span>
          </>
        ) : (
          <>
            <span className="flag">🇬</span>
            <span>English</span>
            <span className="switch-to">KA</span>
          </>
        )}
      </button>
    </div>
  );
};

export default LanguageSwitcher;
```

### თარგმანების ჰუქი / Translation Hook

```jsx
// src/hooks/useTranslation.js
import translations from '../translations/index.json';
import { useLanguage } from '../contexts/LanguageContext';

export const useTranslation = () => {
  const { lang } = useLanguage();

  const t = (key) => {
    const keys = key.split('.');
    let value = translations[lang];
    
    for (const k of keys) {
      value = value?.[k];
      if (!value) {
        console.warn(`Translation missing for: ${key}`);
        return key;
      }
    }
    
    return value;
  };

  return { t, lang };
};

// გამოყენება / Usage:
const MyComponent = () => {
  const { t } = useTranslation();
  
  return (
    <div>
      <h1>{t('nav.home')}</h1>
      <p>{t('common.loading')}</p>
    </div>
  );
};
```

---

## 9️⃣ მთავარი გვერდი / MAIN APP STRUCTURE

```jsx
// src/App.js
import React from 'react';
import { Routes, Route } from 'react-router-dom';
import { HelmetProvider } from 'react-helmet-async';
import { LanguageProvider } from './contexts/LanguageContext';
import Layout from './components/Layout';
import HomePage from './pages/HomePage';
import ProductPage from './pages/ProductPage';
import CartPage from './pages/CartPage';
import ErrorBoundary from './components/ErrorBoundary';

function App() {
  return (
    <HelmetProvider>
      <LanguageProvider>
        <ErrorBoundary>
          <Layout>
            <Routes>
              <Route path="/" element={<HomePage />} />
              <Route path="/products/:id" element={<ProductPage />} />
              <Route path="/cart" element={<CartPage />} />
            </Routes>
          </Layout>
        </ErrorBoundary>
      </LanguageProvider>
    </HelmetProvider>
  );
}

export default App;
```

---

## 🔟 საბოლოო რჩევები / FINAL TIPS

### SEO ოპტიმიზაცია / SEO Optimization

✅ **Meta Tags:**
- გამოიყენეთ უნიკალური title და description თითოეულ გვერდზე
- დაამატეთ Open Graph და Twitter Card meta tags
- გამოიყენეთ canonical URLs

✅ **JSON-LD:**
- დაამატეთ Product schema პროდუქტის გვერდებზე
- გამოიყენეთ BreadcrumbList ნავიგაციისთვის
- დაამატეთ Organization schema მთავარ გვერდზე

✅ **Performance:**
- შეკუმშეთ სურათები (WebP ფორმატი)
- გამოიყენეთ lazy loading სურათებისთვის
- აკეთეთ code splitting

✅ **Debugging:**
- გამოიყენეთ React DevTools
- დაამატეთ Error Boundaries
- მონიტორინგი გაუკეთეთ performance-ს

---

## 📚 რესურსები / RESOURCES

- **React Helmet Async:** https://github.com/staylor/react-helmet-async
- **Schema.org:** https://schema.org/
- **React DevTools:** https://react.dev/learn/react-developer-tools
- **JSON-LD:** https://json-ld.org/
- **Google Structured Data Testing:** https://search.google.com/structured-data/testing-tool

---

## 🎯 დასასრული / CONCLUSION

ეს პრეზენტაცია მოგაწოდეთ სრული სახელმძღვანელო:
- ✅ React პროექტის დაყენება SEO-ით
- ✅ Meta tags და JSON-LD იმპლემენტაცია
- ✅ მონაცემების მართვა JSON ფაილებიდან
- ✅ E-commerce აპლიკაციის აგება
- ✅ დებაგინგის ტექნიკები
- ✅ მულტიენოვანი მხარდაჭერა (ქართული/ინგლისური)

**წარმატებები თქვენს პროექტში! / Good luck with your project!** 🚀

---

გჭირდებათ დამატებითი ინფორმაცია რომელიმე კონკრეტულ თემაზე?